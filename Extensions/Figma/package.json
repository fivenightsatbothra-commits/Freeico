{
  "name": "freeico-figma",
  "version": "1.0.0",
  "description": "Freeico Figma plugin",
  "private": true,
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=code.js",
    "watch": "esbuild code.ts --bundle --outfile=code.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
